<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - AI Apps and Agent in a Day (Settings)</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        .secret-card {
            border-left: 4px solid #28a745;
        }
        .config-card {
            border-left: 4px solid #007bff;
        }
        .badge-secret {
            background-color: #28a745;
        }
        .badge-config {
            background-color: #007bff;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                    <i class="fas fa-cogs"></i> Lab Portal
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <div class="navbar-nav me-auto">
                        <a class="nav-link" href="@Url.Action("Index", "Home")">
                            <i class="fas fa-home"></i> Home
                        </a>
                        <a class="nav-link" href="@Url.Action("Settings", "Home")">
                            <i class="fas fa-cog"></i> Settings
                        </a>
                        <a class="nav-link" href="@Url.Action("AzureLogin", "Home")">
                            <i class="fas fa-key"></i> Azure Login
                        </a>
                    </div>
                    <div class="navbar-nav">
                        <a class="nav-link" href="/swagger">
                            <i class="fas fa-book"></i> API Docs
                        </a>
                        <a class="nav-link" href="/api/configuration/health">
                            <i class="fas fa-heartbeat"></i> Health
                        </a>
                    </div>
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2024 - AI Apps and Agent in a Day (Settings)
        </div>
    </footer>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
